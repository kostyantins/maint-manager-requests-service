package com.example.requestsservice.repository;

import com.example.requestsservice.domain.MaintCopy;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MaintCopyRepository extends JpaRepository<MaintCopy, Long> {
}
